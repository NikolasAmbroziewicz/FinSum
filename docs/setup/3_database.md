# Data Base Setup

We use PostgreSQL and Elastic Search. I recommend to run database on docker

How install docker:

```ts
brew install docker
```

How run Docker:

```
cd backend
docker compose up
```